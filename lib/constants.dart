import 'package:flutter/material.dart';

ThemeData theme = ThemeData().copyWith(
    scaffoldBackgroundColor: Colors.white,
    textTheme: TextTheme(displaySmall: TextStyle(fontFamily: 'Cairo-Regular')));
